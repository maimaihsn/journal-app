# Daily Journal アプリ 設計書

最終更新: 2026-05-20

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| アプリ名 | Daily Journal |
| 目的 | 日々の思考・行動・目標を気軽に記録する |
| 形式 | シングル HTML ファイル（サーバー不要） |
| 動作環境 | モダンブラウザ（PC・スマホ両対応） |
| データ保存 | ブラウザ localStorage（ローカル完結） |
| 外部依存 | なし（ライブラリ・フレームワーク不使用） |

---

## 2. ファイル構成

```
journal-app/
├── index.html   # アプリ本体（HTML + CSS + JS がすべて含まれる）
└── DESIGN.md    # 本設計書
```

---

## 3. 機能一覧

### 3-1. セクション一覧

セクションは **「蓄積」** と **「日別」** の 2 種類に分類される。

| セクション | 分類 | データ型 | 説明 |
|-----------|------|---------|------|
| やりたいこと | 蓄積 | 文字列リスト | 日付に関係なく共通で蓄積される |
| ほしいもの | 蓄積 | 文字列リスト | 日付に関係なく共通で蓄積される |
| 目標 | 蓄積 | 文字列リスト | 日付に関係なく共通で蓄積される |
| 今日のToDo | 日別 | チェックリスト | 日付ごとに独立。完了チェック付き |
| 今日やったこと | 日別 | 文字列リスト | 日付ごとに独立 |
| ジャーナリング | 日別 | 自由テキスト | 日付ごとに独立。自動保存（0.6秒） |

### 3-2. 画面・ビュー

| ビュー | 説明 |
|--------|------|
| メインビュー | タブ切り替えで各セクションを表示・編集 |
| 一覧ビュー | 日別データを日付カード形式で一覧表示 |
| 設定パネル | Discord Webhook URL の入力・保存 |

### 3-3. 操作機能

| 機能 | 詳細 |
|------|------|
| アイテム追加 | 入力欄にテキストを入力し「追加」ボタン or Enter キー |
| アイテム削除 | 各アイテム右端の × ボタン |
| ToDo 完了切替 | チェックボックスをクリック（打ち消し線で視覚表示） |
| 日付ナビゲーション | ‹ › ボタンで前後の日、「今日」ボタンで当日に移動 |
| 一覧表示 | 「📋 一覧」ボタンでビュー切替 |
| カード折りたたみ | 一覧ビューの各日付カードをクリックで開閉 |
| ジャーナル展開 | 一覧ビューで 4 行を超えるジャーナルを「もっと見る」で全文表示 |
| Discord 通知 | アイテム追加時に指定チャンネルへ Webhook 通知 |
| テスト送信 | 設定パネルから Webhook の疎通確認 |

---

## 4. データ設計

### 4-1. localStorage キー一覧

| キー | 用途 |
|------|------|
| `daily-journal-v2` | アプリのすべてのデータ（蓄積 + 日別） |
| `daily-journal-settings` | 設定データ（Webhook URL） |

### 4-2. メインストア構造（`daily-journal-v2`）

```json
{
  "persistent": {
    "wantToDo": ["やりたいこと1", "やりたいこと2"],
    "wishlist":  ["ほしいもの1"],
    "goals":     ["目標1", "目標2"]
  },
  "daily": {
    "YYYY-MM-DD": {
      "todos": [
        { "text": "タスク名", "done": false },
        { "text": "完了タスク", "done": true }
      ],
      "didToday": ["やったこと1", "やったこと2"],
      "journal":  "自由テキスト..."
    }
  }
}
```

#### persistent（蓄積データ）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `wantToDo` | `string[]` | やりたいことのリスト |
| `wishlist` | `string[]` | ほしいものリスト |
| `goals` | `string[]` | 目標リスト |

#### daily[date]（日別データ）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| `todos` | `{text: string, done: boolean}[]` | ToDo チェックリスト |
| `didToday` | `string[]` | 今日やったことリスト |
| `journal` | `string` | ジャーナル自由テキスト |

### 4-3. 設定データ（`daily-journal-settings`）

```json
{
  "webhookUrl": "https://discord.com/api/webhooks/..."
}
```

### 4-4. データバージョンとマイグレーション

| バージョン | キー | 状態 |
|-----------|------|------|
| v1 | `daily-journal-v1` | 廃止。起動時に v2 へ自動マイグレーション後削除 |
| v2 | `daily-journal-v2` | 現行 |

#### v1 → v2 マイグレーション戦略

- **蓄積データ（やりたいこと・ほしいもの・目標）**: v1 では日付ごとにコピーされていたため、最新日付の非空データを採用する
- **日別データ（ToDo・やったこと・ジャーナル）**: 日付ごとにそのまま移行。データがない日はスキップ
- **マイグレーション完了後**: v1 キーを localStorage から削除

---

## 5. UI 設計

### 5-1. 画面レイアウト

```
┌──────────────────────────────┐
│  Daily Journal   [📋一覧][⚙️] │  ← ヘッダー
│  ‹  2026年5月20日（水）  › 今日 │  ← 日付ナビ（一覧時は非表示）
├──────────────────────────────┤
│  [設定パネル] ← ⚙️クリックで開閉  │
├──────────────────────────────┤
│ 蓄積 [やりたいこと][ほしいもの][目標]│  ← 蓄積タブ群（緑系）
│ 日別 [今日のToDo][やったこと][ジャーナル]│  ← 日別タブ群（青系）
├──────────────────────────────┤
│                              │
│       アクティブパネル          │
│                              │
└──────────────────────────────┘
         [トースト通知]
```

### 5-2. カラーパレット

| 変数名 | 値 | 用途 |
|--------|-----|------|
| `--bg` | `#f7f6f3` | 背景色（温かみのあるオフホワイト） |
| `--surface` | `#ffffff` | カード・パネル背景 |
| `--border` | `#e4e2dc` | 境界線 |
| `--text` | `#1c1c1c` | 本文テキスト |
| `--text-sub` | `#888` | 補助テキスト・ラベル |
| `--accent` | `#5b7fa6` | 日別グループ（青系）、ボタン、フォーカス |
| `--accent-light` | `#eef2f8` | 日別アクセントの薄色 |
| `--accent2` | `#6a9e7f` | 蓄積グループ（緑系） |
| `--accent2-light` | `#eef5f1` | 蓄積アクセントの薄色 |
| `--done` | `#aaa` | 完了済みテキスト |
| `--danger` | `#c0392b` | 削除ボタンホバー、エラートースト |

### 5-3. タブグループ

タブは 2 グループに分かれ、グループラベルチップで視覚的に区別する。

| グループ | ラベル | アクティブ色 | 含まれるタブ |
|---------|------|------------|------------|
| 蓄積 | 緑チップ（`蓄積`） | `--accent2`（緑） | やりたいこと・ほしいもの・目標 |
| 日別 | 青チップ（`日別`） | `--accent`（青） | 今日のToDo・今日やったこと・ジャーナル |

### 5-4. 一覧ビュー（カード仕様）

```
┌────────────────────────────────────┐
│ 2026年5月20日（水）  ToDo 2/3  やったこと 2件  ▼ │  ← クリックで折りたたみ
├────────────────────────────────────┤
│ 📋 今日のToDo                        │
│   ✅ 完了タスク（打ち消し線）           │
│   ○  未完了タスク                     │
│ ✨ 今日やったこと                      │
│   • やったこと1                       │
│ 📖 ジャーナル                         │
│   本文（最大4行）...                   │
│   [もっと見る]                        │
└────────────────────────────────────┘
```

- 日付は新しい順（降順）で表示
- データが空の日はカード自体を表示しない
- ジャーナルは4行でクランプ、「もっと見る」で全文展開

---

## 6. 主要な処理フロー

### 6-1. アイテム追加フロー

```
ユーザー入力 → Enter or 追加ボタン
    ↓
addItem(key)
    ├─ 蓄積キー（wantToDo / wishlist / goals）
    │     → loadPersistent() → push → savePersistent()
    ├─ todos
    │     → loadDay(date) → push {text, done:false} → saveDay(date)
    └─ その他日別キー（didToday）
          → loadDay(date) → push → saveDay(date)
    ↓
render()  // 画面更新
    ↓
notify(key, val)  // NOTIFY_KEYS に含まれる場合のみ Discord 送信
```

### 6-2. ジャーナル自動保存フロー

```
テキストエリア input イベント
    ↓
「編集中...」表示
    ↓
デバウンス（600ms）
    ↓
loadDay(date) → data.journal = value → saveDay(date)
    ↓
「保存済み ✓」表示（2秒後にクリア）
```

### 6-3. Discord 通知フロー

```
notify(key, text)
    ↓
loadSettings() → webhookUrl が未設定なら即リターン
    ↓
fetch(webhookUrl, POST, Embed ペイロード)
    ↓
res.ok / 204 → showToast('送信しました ✓')
res.error    → showToast('送信失敗', error=true)
fetch失敗     → showToast('ネットワークエラー', error=true)
```

#### Discord Embed ペイロード例

```json
{
  "embeds": [{
    "title": "✨ やりたいことに追加しました",
    "description": "登録したテキスト",
    "color": 6987391,
    "footer": { "text": "Daily Journal  •  2026年5月20日（水）" }
  }]
}
```

| セクション | emoji | color |
|-----------|-------|-------|
| やりたいこと | ✨ | `#6a9e7f`（緑） |
| 今日のToDo | ✅ | `#5b7fa6`（青） |

---

## 7. JS モジュール構成

```
index.html <script>
│
├─ [定数]
│   STORAGE_KEY / SETTINGS_KEY
│   PERSIST_KEYS / DAILY_KEYS / NOTIFY_KEYS / SECTION_LABELS
│
├─ [ストレージ層]
│   loadStore / saveStore
│   loadPersistent / savePersistent
│   loadDay / saveDay
│   allDailyDates
│
├─ [マイグレーション]
│   migrateV1
│
├─ [状態]
│   currentDate / activeTab / saveTimer / historyMode
│
├─ [ユーティリティ]
│   todayStr / pad / shiftDate / formatDisplay / esc
│
├─ [レンダリング（メインビュー）]
│   render / renderList
│
├─ [アクション]
│   addItem / deleteItem / toggleTodo
│   journal input イベント（自動保存）
│
├─ [一覧ビュー]
│   renderHistory / toggleCard / toggleJournal
│
├─ [ビュー切替]
│   setHistoryMode
│
├─ [タブ制御]
│   tab-btn click イベント
│
├─ [日付ナビ]
│   prev-btn / next-btn / today-btn click イベント
│
├─ [設定]
│   loadSettings / saveSettings / initSettings
│
├─ [Discord 通知]
│   notify / testWebhook
│
├─ [トースト]
│   showToast
│
└─ [初期化]
    migrateV1() → initSettings() → render()
```

---

## 8. 設計上の判断メモ

| 判断 | 理由 |
|------|------|
| シングル HTML ファイル | インストール不要、ダブルクリックで即起動 |
| localStorage | サーバー不要・プライベート・無料 |
| 蓄積/日別の分離（v2） | やりたいこと等は「リスト管理」として日付を跨いで使うのが自然 |
| `Object.assign({}, defaults, saved)` | 新フィールド追加時に既存データでクラッシュしないための防御的マージ |
| ジャーナルのデバウンス 600ms | キーストロークごとに保存せず、体感遅延なく保存できるバランス |
| Discord は `fetch` 直接呼び出し | バックエンド不要。Discord Webhook は CORS 対応済み |
| ストレージキーを v1 → v2 に変更 | 構造破壊的変更のため別キーで運用し、起動時マイグレーションで引き継ぐ |
| 最大幅 600px | PC でもスマホでも読みやすい幅 |
